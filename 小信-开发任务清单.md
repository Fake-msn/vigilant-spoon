# 小信 · 开发任务清单（可被开发工具直接消费）

> 版本：v3.0 · 2026-08-05
> 用途：本清单按依赖拓扑排序，开发工具（或人工）**逐条执行指令**；每条指令自包含（动作 + 文件 + 要点 + 即时验证）。
> 配套：`小信-前端开发任务计划书.md` / `小信-后端开发任务计划书.md`（第 0 章契约为唯一真源，本清单引用编号不重复定义）

---

## 0. 消费说明（开发工具先读）

1. **执行顺序**：按 Phase 0→5 顺序；同 Phase 内标注 `∥` 的两条线可并行（不同端，互不阻塞）。
2. **模块门禁**：每模块由若干指令组成。模块内所有指令完成后：
   ```
   自动检查（lint+typecheck / ruff+mypy）→ 测试（vitest / pytest）→ 通过后提交人工审核 → 审核通过 → 下一模块
   ```
   任何一步失败 → 打回修，不进入下一模块。
3. **契约真源**：接口定义在后端 `app/schemas/`（pydantic）。**禁止在任一端手工重定义接口**；前端类型用 `npm run gen:types` 生成。契约变更流程：改 pydantic → 重启后端 → 前端重新 gen:types → 双端跑对应模块测试。
4. **WS 协议铁律**：上行=二进制 PCM16 帧（非 base64/JSON）；下行=JSON 事件（audio base64）。见指令 BE-B1-02。
5. **RAG 铁律**：向量检索只允许出现在「开场素材 / 备课 / 评语检索」；**对话中不开放 RAG 工具**（BE-B3 断言）。
6. **宠物状态铁律**：状态转移为纯函数 + 约束断言，零 LLM（BE-B4-04）。
7. **演示铁律**：演示只走确定性字段（理想/承诺/行动），不赌 RAG 召回；引用准确率口径=「关键字段 100% + 对话回顾 ≥95%」。

---

## 1. 执行拓扑

```
Phase 0  契约           BE-B0(schemas) ──gen:types──▶ FE-M0
Phase 1  并行启动        BE-B1(spike) BE-B2(身份)   ∥   FE-M1(身份页)
Phase 2  语音主线        BE-B3(语音服务)  ←依赖B1    ∥   FE-M2(WS层) FE-M3(语音页)
Phase 3  结算链          BE-B4(结算/宠物)  ←依赖B3   ∥   FE-M4(档案/宠物)
Phase 4  写信/教师端     BE-B5(写信+RAG) BE-B6(课堂) ∥   FE-M6(信件) FE-M5(教师台)
Phase 5  联调验收        BE-B7(指标/日志)            ∥   FE-M7(联调)
```

接口就绪：B2→M1 · B3→M2/M3 · B4→M4 · B5→M6 · B6→M5。

---

## 2. Phase 0 · 契约定稿（先于一切）

### BE-B0 · 后端骨架 + 契约（⬜）

| ID | 动作 | 文件 | 要点 | 即时验证 |
|---|---|---|---|---|
| BE-B0-01 | 初始化 FastAPI 工程 | `pyproject.toml` `app/main.py` | Python 3.12；依赖：fastapi/uvicorn/agentscope/pydantic/tenacity/apscheduler/httpx | `uvicorn app.main:app` 启动无错 |
| BE-B0-02 | 定义全部 pydantic 契约 | `app/schemas/*.py` | §契约 R1-R13 全量：ClassInfo/EnterReq/EnterResp/StudentProfile/GrowthView/Commitment/PetState/ScoreCard/EvidenceItem/JobRef/JobStatus/Letter/LessonPlan/ClassroomStatus；`session_id` 加唯一约束（幂等 key） | `python -c "from app.schemas import *"` 无错 |
| BE-B0-03 | SQLite + growth 主表迁移 | `app/db/migrations/` `app/db/*.py` | 字段：ideal/commitments(JSON)/actions(JSON)/stage/pet_state(JSON)/last_gist/needs_care/teacher_constraints；**转写与音频不入主表**（走 L2/L3 分层）；迁移幂等 | 迁移跑两次不报错 |
| BE-B0-04 | 演示数据 seed | `scripts/seed.py` | 1 个班级 + 5 学生（含理想/承诺/宠物状态）；可重复执行 | seed 后 `SELECT` 能查到 5 学生 |
| BE-B0-05 | 暴露 OpenAPI | `app/main.py` | 挂 routers 空壳 + `/api/openapi.json` | GET `/api/openapi.json` 含 R1-R13 路径 |
| BE-B0-06 | 模块测试 | `tests/b0/` | 用例：①openapi 全路径 ②迁移幂等 ③seed 可查 | `pytest tests/b0 -q` 全绿 |

**门禁**：✅ruff+mypy → 🧪pytest → 🙋审核（契约与双计划书 §0 一致）→ 🏁

### FE-M0 · 前端脚手架 + 契约类型（⬜）

| ID | 动作 | 文件 | 要点 | 即时验证 |
|---|---|---|---|---|
| FE-M0-01 | Vite+React+TS 脚手架 | `package.json` `vite.config.ts` `tsconfig.json` | React 18+TS5；安装 vitest/@testing-library/react | `npm run dev` 出默认页 |
| FE-M0-02 | 接入类型生成 | `scripts/gen-types.mjs` | 拉取后端 `/api/openapi.json` → 生成 `src/types/generated.ts`（openapi-typescript） | `npm run gen:types` 产出文件 |
| FE-M0-03 | vitest 配置 | `vitest.config.ts` | 环境 jsdom；别名 `@/` | `npx vitest run` 空跑通过 |
| FE-M0-04 | 目录骨架 | `src/{api,ws,stores,pages,components,features,__tests__}/` | 与前端计划书 §1 一致 | 目录存在 |

**门禁**：✅lint+typecheck → 🧪vitest → 🙋审核（类型为生成物非手抄）→ 🏁

---

## 3. Phase 1 · 并行启动（BE-B1/B2 ∥ FE-M1）

### BE-B1 · AgentScope spike + WS 二进制适配（最高风险，⬜）

| ID | 动作 | 文件 | 要点 | 即时验证 |
|---|---|---|---|---|
| BE-B1-01 | 验证 RealtimeAgent 连通 | `scripts/spike_voice.py` | `RealtimeAgent(name, sys_prompt, model=DashScopeRealtimeModel(model_name="qwen3-omni-flash-realtime", voice=..., enable_input_audio_transcription=True))`；`agent.start(outgoing_queue)`；`agent.handle_input(ClientEvents)` | 一条语音进、一段语音回 |
| BE-B1-02 | 二进制上行适配层 | `app/ws/adapter.py` | `websocket.receive_bytes()` → PCM16 帧(≤80ms/2560B) → 构造音频输入事件 → `handle_input`；下行 `queue.get() → websocket.send_json(msg.model_dump())` | 适配层收 2560B 帧不抛错 |
| BE-B1-03 | 首响计时探针 | `app/ws/probe.py` | 记录 `vad_end 事件时刻` → `首个 audio_chunk 时刻`，10 轮采样输出 P50/P90 | 探针可导出 CSV |
| BE-B1-04 | spike 结论文档 | `docs/spike-voice.md` | 记录：能否走通、P50 实测、**若二进制失败→已降级官方 JSON 示例**的决策 | 文档有数据 |
| BE-B1-05 | 模块测试 | `tests/b1/` | 用例：①适配层帧处理 ②10 轮 P50<2s（真 API，超时跳过并记录）③下行事件序列完整 | `pytest tests/b1 -q` |

**门禁**：✅ → 🧪 → 🙋（spike 结论真实 + 降级决策明确）→ 🏁

### BE-B2 · 身份 + 班级路由（⬜）

| ID | 动作 | 文件 | 要点 | 即时验证 |
|---|---|---|---|---|
| BE-B2-01 | R1 班级+名单 | `app/routers/classes.py` | `GET /api/classes/{code}` → ClassInfo | curl 返回 JSON |
| BE-B2-02 | R2 身份进入 | `app/routers/session.py` | `POST /api/session/enter`：校验班级码+姓名 → 生成 session_token → 返回 EnterResp（含 light 档案） | 正确请求返回 token |
| BE-B2-03 | 错误分支 | 同上 | 班级码不存在 404；姓名不在名单→返回可选列表；重复进入返回新 token 不破坏旧会话 | 三用例可断言 |
| BE-B2-04 | 模块测试 | `tests/b2/` | 用例：①正确进入 ②404 ③未知姓名 ④重复进入 | `pytest tests/b2 -q` 全绿 |

**门禁**：✅ → 🧪 → 🙋（无账号/PIN 边界 + light 视图无敏感字段）→ 🏁

### FE-M1 · 路由骨架 + 身份选择（⬜）

| ID | 动作 | 文件 | 要点 | 即时验证 |
|---|---|---|---|---|
| FE-M1-01 | 路由 | `src/router.tsx` | `/`→身份选择；`/student`→学生端；`/teacher`→教师端；无 token 访问受保护路由→重定向 `/` | 路由切换正常 |
| FE-M1-02 | 身份页 | `src/pages/identity/` | 班级码+姓名输入 → 调 R1/R2 → 存 session_token/profile（sessionStorage） | 正确输入跳转学生端 |
| FE-M1-03 | 错误态 | 同上 | 班级码错误显示错误；姓名可下拉选择 | 错误分支可见 |
| FE-M1-04 | 模块测试 | `src/__tests__/m1/` | 用例：①成功跳转 ②错误班级码 ③无 token 重定向 | `npx vitest run src/__tests__/m1` 全绿 |

**门禁**：✅ → 🧪 → 🙋（身份边界 + token 存储位置）→ 🏁

---

## 4. Phase 2 · 语音主线（BE-B3 ∥ FE-M2 → FE-M3）

### BE-B3 · RealtimeAgent 服务（⬜）

| ID | 动作 | 文件 | 要点 | 即时验证 |
|---|---|---|---|---|
| BE-B3-01 | 三角色注册表 | `app/services/voice.py` | `RoleConfig`：classroom/journal/lesson 各持 sys_prompt + toolkit + injection_fields；加角色=加配置 | 三模式配置可枚举 |
| BE-B3-02 | WS 端点 | `app/ws/endpoint.py` | `/ws/voice?session_token=&mode=`：校验 token → 查 light 档案 → 拼 sys_prompt → start → 循环转发；断开时 agent.stop 无泄漏 | 单轮语音往返成功 |
| BE-B3-03 | pre-stream 注入 | 同上 | 注入理想/承诺/gist/宠物/教师约束，**≤500 token**；开场白=确定性字段 + RAG 素材（仅开场） | 断言注入字段在 sys_prompt 中 |
| BE-B3-04 | 对话中无 RAG | 同上 | toolkit 列表断言：**对话轮次内无 RAG 检索工具** | 断言通过 |
| BE-B3-05 | 模块测试 | `tests/b3/` | 用例：①三模式 sys_prompt 不同 ②注入 ≤500 token ③无 RAG 工具 ④WS 断开无泄漏 | `pytest tests/b3 -q` 全绿 |

**门禁**：✅ → 🧪 → 🙋（pre-stream 官方通道 + RAG 铁律 + 二进制适配真实可用）→ 🏁

### FE-M2 · API client + WS 协议层（⬜）

| ID | 动作 | 文件 | 要点 | 即时验证 |
|---|---|---|---|---|
| FE-M2-01 | REST client | `src/api/client.ts` | 统一 baseURL/超时/错误处理；方法对应 R1-R13 | mock 单测可断言 |
| FE-M2-02 | WS 连接管理 | `src/ws/voice.ts` | 连接/关闭/断线重连（不丢 session_token）；订阅制事件分发 | 连接与重连用例过 |
| FE-M2-03 | 二进制上行 | 同上 | PCM16 帧封装（16k/16bit/mono/≤80ms）→ `ws.send(ArrayBuffer)` | 帧字节数断言正确 |
| FE-M2-04 | 下行事件分发 | 同上 | session_started/audio_chunk/transcript/turn_end/error → 订阅者 | 事件顺序正确 |
| FE-M2-05 | 模块测试 | `src/__tests__/m2/` | 用例：①帧封装 ②audio 顺序播放 ③重连 ④错误冒泡 | `npx vitest run src/__tests__/m2` 全绿 |

**门禁**：✅ → 🧪 → 🙋（上行确为二进制 + 分发订阅制）→ 🏁

### FE-M3 · 语音对话页（⬜）

| ID | 动作 | 文件 | 要点 | 即时验证 |
|---|---|---|---|---|
| FE-M3-01 | 音频采集 | `src/features/voice-session/audio.ts` | Web Audio 采集 → PCM16 编码 → 发送 | mock AudioContext 用例过 |
| FE-M3-02 | 播放队列 | `src/features/voice-session/player.ts` | audio_chunk 按 seq 顺序播放；乱序不破 | 乱序用例过 |
| FE-M3-03 | 对话组件 | `src/features/voice-session/VoiceSession.tsx` | 开场白展示、录音状态/AI 说话状态互斥、转写流显示、mode 透传 | 三模式可复用 |
| FE-M3-04 | 错误态 | 同上 | error 事件显示提示，不崩溃 | 用例过 |
| FE-M3-05 | 模块测试 | `src/__tests__/m3/` | 用例：①采集编码链路 ②乱序播放 ③turn_end 恢复 ④错误提示 | `npx vitest run src/__tests__/m3` 全绿 |

**门禁**：✅ → 🧪 → 🙋（不抢话 + 可复用三模式）→ 🏁

---

## 5. Phase 3 · 结算链（BE-B4 ∥ FE-M4）

### BE-B4 · 结算链 + 宠物状态机（⬜）

| ID | 动作 | 文件 | 要点 | 即时验证 |
|---|---|---|---|---|
| BE-B4-01 | 评分服务 | `app/services/scoring.py` | LLM 吃**转写全文**（非摘要）→ ScoreCard（维度分 + evidence：rubric_id + 带时间戳 quote_span + source） | 输出符合 schema |
| BE-B4-02 | 结算链编排 | `app/services/settle.py` | **30 行**：评分 → 写档案 → 宠物 delta；DB 事务包裹；幂等 key=session_id | 重跑不重复加分 |
| BE-B4-03 | 档案路由 | `app/routers/growth.py` | R3（view=full/light）、R4 宠物、R5 异步出图、R6 job 轮询 | 四接口可用 |
| BE-B4-04 | 宠物状态机 | `app/services/pet.py` | 纯函数 + 断言：值域 clamp(±15/次)、相邻迁移、保护性断言（不羞辱）、低频字段隔离、敏感词闸门；**零 LLM** | 单测穷举转移 |
| BE-B4-05 | needs_care 契约 | 同上 | vitality 下行 → 置 needs_care（供写信必覆盖 + 前端即时文案） | 断言置位 |
| BE-B4-06 | 模块测试 | `tests/b4/` | 用例：①结算落库 ②幂等 ③clamp ④保护断言 ⑤evidence span 对齐 ⑥吃全文 ⑦needs_care | `pytest tests/b4 -q` 全绿 |

**门禁**：✅ → 🧪 → 🙋（零 LLM 状态机 + 幂等 + 可申诉 evidence）→ 🏁

### FE-M4 · 档案 + 宠物页（⬜）

| ID | 动作 | 文件 | 要点 | 即时验证 |
|---|---|---|---|---|
| FE-M4-01 | 档案页 | `src/pages/growth/` | R3 full 视图：理想/承诺/行动/成长值/阶段 | mock 渲染一致 |
| FE-M4-02 | 宠物组件 | `src/components/PetView.tsx` | R4 轮询（退避）；PetState → 形象 + 状态 | vitality 下降显示文案 |
| FE-M4-03 | 肖像生成 | 同上 | R5→R6 轮询：pending/running/done/failed 态 | done 显示画像 |
| FE-M4-04 | 即时文案 | 同上 | needs_care 时显示规则文案（非 LLM） | 断言文案 |
| FE-M4-05 | 模块测试 | `src/__tests__/m4/` | 用例：①档案渲染 ②宠物状态文案 ③job 轮询 ④failed 重试 | `npx vitest run src/__tests__/m4` 全绿 |

**门禁**：✅ → 🧪 → 🙋（宠物/肖像解耦 + 轮询退避 + 规则文案）→ 🏁

---

## 6. Phase 4 · 写信 / 教师端（BE-B5/B6 ∥ FE-M6/M5）

### BE-B5 · 写信 job + RAG 外围（⬜）

| ID | 动作 | 文件 | 要点 | 即时验证 |
|---|---|---|---|---|
| BE-B5-01 | 写信模板引擎 | `app/services/letter.py` | 首版模板：读档案填槽位（理想/承诺/完成情况）；needs_care 学生必含关怀段 | 生成信含关怀关键词 |
| BE-B5-02 | job 注册 | `app/jobs/letter_job.py` | APScheduler 每周 cron + R8 手动触发；tenacity：429/超时可重试、schema 错不可重试 | 手动触发落库 |
| BE-B5-03 | 信件路由 | `app/routers/letters.py` | R7 列表、R8 触发生成（返回 JobRef） | 两接口可用 |
| BE-B5-04 | RAG 外围 | `app/services/rag.py` | 向量库只存素材（历史摘要/gist/评语/备课），**不存状态字段**；仅开场/备课/评语检索 | 检索不泄漏状态 |
| BE-B5-05 | 模块测试 | `tests/b5/` | 用例：①触发生成 ②needs_care 覆盖 ③重试模拟 429 ④RAG 无泄漏 | `pytest tests/b5 -q` 全绿 |

**门禁**：✅ → 🧪 → 🙋（首版模板 + 重试策略 + RAG 边界）→ 🏁

### BE-B6 · 课堂控制 + 备课模式（⬜）

| ID | 动作 | 文件 | 要点 | 即时验证 |
|---|---|---|---|---|
| BE-B6-01 | 备课路由 | `app/routers/lesson.py` | R9：lesson 角色模式（topic/goals→LessonPlan）；R10 查看；**同一 RealtimeAgent 非新 Agent** | 生成结构完整 |
| BE-B6-02 | 课堂状态机 | `app/routers/classroom.py` | idle/active/paused 迁移表（确定性，非 LLM 判定） | 非法迁移被拒 |
| BE-B6-03 | 课堂接口 | 同上 | R11 start / R12 control（pause/resume/next_student/switch_content）/ R13 轮询 | 状态与动作一致 |
| BE-B6-04 | 模块测试 | `tests/b6/` | 用例：①备课结构 ②状态迁移 ③switch_content ④轮询一致 | `pytest tests/b6 -q` 全绿 |

**门禁**：✅ → 🧪 → 🙋（确定性状态机 + 备课=角色模式）→ 🏁

### FE-M6 · 信件页 + 首响测量（⬜）

| ID | 动作 | 文件 | 要点 | 即时验证 |
|---|---|---|---|---|
| FE-M6-01 | 信件列表 | `src/pages/feedback/` | R7 列表 + 空态 | 空态渲染 |
| FE-M6-02 | 触发生成 | 同上 | R8 触发 → job 状态显示 | 状态可见 |
| FE-M6-03 | 首响探针工具 | `src/tools/latency-probe.ts` | 起算点=服务端 vad_end 事件 → 首个 audio_chunk；10 轮 P50/P90；导出 CSV | 数据可导出 |
| FE-M6-04 | 模块测试 | `src/__tests__/m6/` | 用例：①空态 ②触发 ③P50/P90 计算 | `npx vitest run src/__tests__/m6` 全绿 |

**门禁**：✅ → 🧪 → 🙋（起算点=服务端 vad_end，非手松）→ 🏁

### FE-M5 · 教师工作台（⬜）

| ID | 动作 | 文件 | 要点 | 即时验证 |
|---|---|---|---|---|
| FE-M5-01 | 备课表单 | `src/pages/teacher/` | R9 生成课程 → R10 查看；topic 非空校验 | 校验生效 |
| FE-M5-02 | 课堂控制面板 | 同上 | R11 发起 / R12 控制 / R13 轮询；按钮状态随 ClassroomStatus 切换；乐观更新有回滚 | 状态同步 |
| FE-M5-03 | 学生列表 | 同上 | R3 light 视图摘要列表 | 渲染正常 |
| FE-M5-04 | 卸载清理 | 同上 | 页面卸载停止轮询 | 无泄漏断言 |
| FE-M5-05 | 模块测试 | `src/__tests__/m5/` | 用例：①表单校验 ②状态切换 ③列表 ④卸载停轮询 | `npx vitest run src/__tests__/m5` 全绿 |

**门禁**：✅ → 🧪 → 🙋（投屏场景 + 状态与服务端一致）→ 🏁

---

## 7. Phase 5 · 联调验收（BE-B7 ∥ FE-M7）

### BE-B7 · 指标复测 + 日志 + 演示数据（⬜）

| ID | 动作 | 文件 | 要点 | 即时验证 |
|---|---|---|---|---|
| BE-B7-01 | step 级日志 | `app/services/settle.py` | 每步 `(step_id, input, output, status)`，job_id 贯穿；ScoreCard evidence 可查 | 日志可追 |
| BE-B7-02 | 指标自动化 | `scripts/acceptance.py` | 首响 P50 / 生图<10s / **关键字段引用 100%（10 次重修开场断言与库一致）** / 信件 100% | 结果写入 `docs/acceptance.md` |
| BE-B7-03 | 演示库清理 | `scripts/seed.py --fresh` | 干净库 + 演示 seed | 无脏数据 |
| BE-B7-04 | 全量回归 | - | `ruff check . && mypy app && pytest -q` | 全绿 |

**门禁**：✅ → 🧪 → 🙋（指标有数据支撑 + 日志完整）→ 🏁

### FE-M7 · 联调打磨 + 演示脚本（⬜）

| ID | 动作 | 文件 | 要点 | 即时验证 |
|---|---|---|---|---|
| FE-M7-01 | 全链路联调 | - | 学生线（进入→对话→档案/宠物）+ 教师线（备课→课堂→写信） | 两线无阻断 |
| FE-M7-02 | 首响体感抽检 | `src/tools/latency-probe.ts` | 起算点 A（学生物理停声）：3 段真实停声→出声视频存档 | 无抢话投诉 |
| FE-M7-03 | 视觉对照 | - | 按设计 token 走查像素风统一 | 无掉链子页面 |
| FE-M7-04 | 全量回归 | - | `npm run lint && npm run typecheck && npx vitest run` | 全绿 |

**门禁**：✅ → 🧪 → 🙋（演示铁律 + 数据支撑）→ 🏁 **【清单终点】**

---

## 8. 验收指标总表（Phase 5 复测）

| 指标 | 目标 | 测量 | 责任 |
|---|---|---|---|
| 语音首响 P50（起算点 B） | <2s | ws probe 10 轮中位数 | BE-B1-03 / FE-M6-03 |
| 首响体感（起算点 A） | 无抢话 | 3 段视频 | FE-M7-02 |
| 图片生成 | <10s | job 计时 | BE-B7-02 |
| 关键字段引用 | 100% | 10 次开场断言 | BE-B7-02 |
| 对话回顾引用 | ≥95% | 人工抽检 | FE-M7 |
| 信件按时生成 | 100% | 触发记录 | BE-B7-02 |
| 宠物状态一致性 | 0 跳变 | 单测穷举+回放 | BE-B4-04 |
| 结算幂等 | 重试不重复 | 同 session 重跑 | BE-B4-02 |

---

## 9. 风险盯防（执行中持续关注）

| 风险 | 触发点 | 预案 |
|---|---|---|
| R1 二进制 WS 适配失败 | BE-B1 | 降级官方 JSON 示例，B3 再优化（已在 BE-B1-04 记录） |
| R2 首响 P50>2s | Phase 2 实测 | 砍注入 token → 调 VAD 阈值 → 帧格式优化 |
| R3 生图>10s | Phase 3 | 出图移异步 + 前端占位 |
| R4 VAD 抢话 | Phase 2 起 | 调静音窗；演示脚本设计"学生连续说完再停" |
| R5 evidence 错位 | Phase 3 | 时间戳 span 对齐，宁缺勿错 |
| R6 周期内做不完 | 每日站会 | 砍单顺序：RAG→课堂面板→宠物多阶段→写信模板 |
