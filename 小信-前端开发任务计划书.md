# 小信 · 前端开发任务计划书

> 版本：v2.0 · 2026-08-05 · 基于 6 轮架构评审收敛决策
> 技术栈：Vite + React + TypeScript + vitest + React Testing Library
> 配套文档：`小信-后端开发任务计划书.md`（第 0 章契约完全一致，变更需双端同步）

---

## 0. 共享接口契约（唯一真源，与后端保持一致）

> ⚠️ 本节为前后端共享契约。任何修改必须同步更新两份计划书第 0 章，并以本文件 + 后端 `schemas/` 的 pydantic 定义为准。前端类型由后端 OpenAPI 生成（见 M0），**禁止手工抄写类型**。

### 0.1 REST 接口总表

| # | 方法 | 路径 | 请求体 | 响应体 | 阶段 |
|---|---|---|---|---|---|
| R1 | GET | `/api/classes/{class_code}` | - | `ClassInfo` | M1 |
| R2 | POST | `/api/session/enter` | `EnterReq` | `EnterResp` | M1 |
| R3 | GET | `/api/students/{id}/growth?view=full\|light` | - | `GrowthView` | M4 |
| R4 | GET | `/api/students/{id}/pet` | - | `PetState` | M4 |
| R5 | POST | `/api/students/{id}/pet/portrait` | - | `JobRef` | M4 |
| R6 | GET | `/api/jobs/{job_id}` | - | `JobStatus` | M4 |
| R7 | GET | `/api/students/{id}/letters` | - | `Letter[]` | M6 |
| R8 | POST | `/api/students/{id}/letters/generate` | - | `JobRef` | M6 |
| R9 | POST | `/api/lesson/generate` | `LessonGenReq` | `LessonPlan` | M5 |
| R10 | GET | `/api/lessons/{id}` | - | `LessonPlan` | M5 |
| R11 | POST | `/api/classes/{code}/session/start` | - | `ClassroomStatus` | M5 |
| R12 | POST | `/api/classes/{code}/session/control` | `ControlReq` | `ClassroomStatus` | M5 |
| R13 | GET | `/api/classes/{code}/session/status` | - | `ClassroomStatus` | M5 |

### 0.2 WebSocket 语音协议

```
WS /ws/voice?session_token={token}&mode={classroom|journal|lesson}
```

- **上行（学生 → 服务端）**：二进制帧，PCM16 @16kHz/16bit/mono，每帧 ≤80ms（约 2560 字节）。**不用 base64/JSON**（省 33% 体积 + 无 JSON 解析）。
- **下行（服务端 → 前端）**：JSON 事件（与 AgentScope `outgoing_queue` 官方路径兼容，音频字段 base64）。

| 下行事件 | 字段 | 说明 |
|---|---|---|
| `session_started` | `opening: string` | 开场白文本（含档案注入），前端可显示 |
| `audio_chunk` | `seq: number, data: base64` | AI 语音音频块，前端追加播放 |
| `transcript` | `text: string, is_final: bool` | 用户语音转写（`enable_input_audio_transcription=True`） |
| `turn_end` | `final: bool` | 一轮结束 |
| `error` | `code: string, message: string` | 错误事件 |

前端通过 `session_token`（R2 返回）建立连接；`mode` 决定后端角色模式（classroom=课堂共教 / journal=日常分享 / lesson=备课）。

### 0.3 核心数据结构（TS 类型 = 后端 OpenAPI 生成，此处为语义说明）

```typescript
interface ClassInfo { class_code: string; class_name: string; students: StudentProfile[] }
interface EnterReq { class_code: string; student_name: string }
interface EnterResp { session_token: string; profile: StudentProfile; view: GrowthView }
interface StudentProfile { student_id: string; name: string; class_code: string; avatar_seed: number }

interface GrowthView {          // ?view=light 时省略 actions/history
  ideal: string | null;
  commitments: Commitment[];
  last_gist: string | null;     // 上次一句话摘要（确定性字段）
  growth_value: number;
  stage: string;
  pet: PetState;
}
interface Commitment { id: string; text: string; created_at: string; status: 'active'|'fulfilled'|'expired' }

interface PetState {
  species: string;              // 职业方向决定外观族系
  stage: number;                // 0..N 相邻迁移
  vitality: number;             // 0..100，单次变化 ≤ ±15（服务端 clamp）
  growth_value: number;
  mood: 'cheerful'|'calm'|'low';
  updated_at: string;
}

interface JobRef { job_id: string }
interface JobStatus { job_id: string; status: 'pending'|'running'|'done'|'failed'; result_url?: string }

interface Letter { letter_id: string; student_id: string; title: string; body: string; generated_at: string; source: 'template'|'llm' }

interface LessonGenReq { topic: string; goals: string[]; guidance: string }
interface LessonPlan { lesson_id: string; topic: string; goals: string[]; guidance_strategy: string; materials: {title: string; content: string}[]; created_at: string }

interface ControlReq { action: 'pause'|'resume'|'next_student'|'switch_content'; payload?: string }
interface ClassroomStatus {
  session_id: string;
  state: 'idle'|'active'|'paused';
  current_student: string | null;
  current_slot: string | null;  // 课程环节
  turn_count: number;
  updated_at: string;
}
```

---

## 1. 前端架构与模块划分

```
src/
├── types/          # 后端 OpenAPI 生成（M0，自动生成禁止手改）
├── api/            # REST client 层（M2，axios/fetch 封装，统一错误处理）
├── ws/             # WS 协议层（M2：连接管理、二进制发送、事件分发）
├── stores/         # 全局状态（session_token/profile 等，M2）
├── pages/          # 路由页面（identity/classroom/journal/growth/feedback/teacher）
├── components/     # 复用组件（PetView/AudioRecorder/LetterCard...）
├── features/       # 功能模块（voice-session 为核心）
└── __tests__/      # vitest 测试（与模块对应）
```

**可扩展性设计（要求 4）**：
- `api/` 与 `ws/` 完全解耦于页面 → 二期加账号体系只改 `stores/auth`，页面零改动。
- 页面按 six-pack 目录组织 → 新增模块=新增目录+路由，不侵入既有页面。
- 所有接口类型来自生成器 → 契约变更时 `npm run gen:types` 全量刷新，编译期暴露不匹配。

---

## 2. 模块任务（门禁：⬜ → 🔨 → ✅自动检查 → 🧪测试 → 🙋审核 → 🏁）

> 每个模块完成后运行「自动检查 + 测试」，通过后**提交人工审核**，审核通过方可进入下一模块。

### M0 · 脚手架 + 契约类型（前置，无依赖）

| 项 | 内容 |
|---|---|
| 文件范围 | `package.json`、`vite.config.ts`、`tsconfig.json`、`src/types/` |
| 任务 | Vite+React+TS 脚手架；vitest 配置；接入 `npm run gen:types`（从后端 `/api/openapi.json` 生成 `src/types/generated.ts`） |
| 自动检查 | `npm run lint && npm run typecheck` |
| 测试 | `npx vitest run src/types` —— 用例：①generated.ts 存在且可编译 ②核心类型（GrowthView/PetState）字段齐全 |
| 审核要点 | ①类型为生成物而非手抄 ②vitest 可运行 ③README 有启动命令 |

### M1 · 路由骨架 + 身份选择（依赖 M0；后端并行 B2）

| 项 | 内容 |
|---|---|
| 文件范围 | `src/pages/identity/`、`src/router.tsx` |
| 任务 | 路由：`/`（身份选择）→ `/student`（学生端）→ `/teacher`（教师端）；身份页调用 R1/R2；进入后存 `session_token` 与 `profile` |
| 自动检查 | `npm run lint && npm run typecheck` |
| 测试 | `npx vitest run src/pages/identity` —— 用例：①输入班级码+姓名后跳转学生端 ②错误班级码显示错误 ③无 token 访问学生端被重定向 |
| 审核要点 | ①身份选择符合"班级码+姓名、无账号"边界 ②token 存储位置明确（sessionStorage） ③路由守卫生效 |

### M2 · API client + WS 协议层（依赖 M0；后端并行 B1/B3）

| 项 | 内容 |
|---|---|
| 文件范围 | `src/api/client.ts`、`src/ws/voice.ts`、`src/stores/session.ts` |
| 任务 | REST client（统一错误/超时/认证头）；WS 封装：连接管理、**二进制上行发送（PCM16 帧）**、事件分发（session_started/audio_chunk/transcript/turn_end/error）、断线重连 |
| 自动检查 | `npm run lint && npm run typecheck` |
| 测试 | `npx vitest run src/ws src/api`（用 mock WebSocket）—— 用例：①PCM16 帧封装字节数正确 ②audio_chunk 拼接播放顺序 ③断线后自动重连且不丢 session ④错误事件冒泡给 UI |
| 审核要点 | ①上行确为二进制帧（无 base64）②事件分发是订阅制（组件不直接依赖 ws 内部）③重连不重置 session |

### M3 · 语音对话页（classroom/journal 共用组件）（依赖 M2；后端并行 B3）

| 项 | 内容 |
|---|---|
| 文件范围 | `src/features/voice-session/`、`src/pages/student/` |
| 任务 | 语音采集（Web Audio，PCM16 编码）、播放队列（audio_chunk 顺序播放）、开场白展示、VAD 状态指示（录音中/等待 AI）、转写文本流显示 |
| 自动检查 | `npm run lint && npm run typecheck` |
| 测试 | `npx vitest run src/features/voice-session` —— 用例：①采集→编码→发送链路（mock AudioContext）②音频块乱序到达仍按 seq 播放 ③turn_end 后恢复可录音状态 ④错误事件显示提示不崩溃 |
| 审核要点 | ①`mode` 参数透传（classroom/journal/lesson）②录音状态与 AI 说话状态互斥（不抢话）③组件可复用于三种模式 |

### M4 · 档案 + 宠物页（依赖 M2；后端并行 B4）

| 项 | 内容 |
|---|---|
| 文件范围 | `src/pages/growth/`、`src/components/PetView.tsx` |
| 任务 | 档案页（理想/承诺列表/成长值/阶段）、宠物展示（R4 轮询）、像素肖像生成（R5→R6 轮询，异步完成态显示）、`needs_care` 时展示即时文案（规则文案，来自后端字段） |
| 自动检查 | `npm run lint && npm run typecheck` |
| 测试 | `npx vitest run src/pages/growth` —— 用例：①档案渲染与 mock 数据一致 ②宠物状态变化（vitality 下降）显示对应文案 ③job 轮询到 done 显示画像 ④job failed 显示重试入口 |
| 审核要点 | ①宠物/肖像两区块独立（不耦合）②轮询有退避不刷爆接口 ③文案为规则生成（非 LLM 自由发挥） |

### M5 · 教师工作台（依赖 M3；后端并行 B5/B6）

| 项 | 内容 |
|---|---|
| 文件范围 | `src/pages/teacher/` |
| 任务 | 备课表单（R9 生成课程→R10 查看）；课堂控制面板（R11 发起 / R12 控制：pause/resume/next_student/switch_content / R13 轮询状态）；学生档案摘要列表（R3 light 视图） |
| 自动检查 | `npm run lint && npm run typecheck` |
| 测试 | `npx vitest run src/pages/teacher` —— 用例：①备课表单校验 topic 非空 ②控制按钮状态随 ClassroomStatus 切换（active/paused）③学生列表显示 light 档案 ④轮询在页面卸载时停止 |
| 审核要点 | ①投屏场景：控制面板状态与服务端一致 ②点名/暂停等操作乐观更新有回滚 ③页面可被大屏投屏（宽屏布局） |

### M6 · 信件页 + 首响测量工具（依赖 M2；后端并行 B5）

| 项 | 内容 |
|---|---|
| 文件范围 | `src/pages/feedback/`、`src/tools/latency-probe.ts` |
| 任务 | 信件列表（R7）+ 手动触发生成（R8）；**首响测量工具**：记录"VAD 判定完成"（起算点 B）到"首个 audio_chunk 到达"的耗时，导出 P50/P90 |
| 自动检查 | `npm run lint && npm run typecheck` |
| 测试 | `npx vitest run src/pages/feedback src/tools` —— 用例：①信件列表空态 ②触发生成后显示 job 状态 ③probe 正确计算 10 次采样的 P50/P90 |
| 审核要点 | ①probe 起算点=服务端 vad_end 事件（非前端手松）②测量数据可导出 CSV 供验收 |

### M7 · 联调打磨 + 验收脚本（依赖 M0-M6 全部通过）

| 项 | 内容 |
|---|---|
| 任务 | 与后端联调全流程；演示脚本两条线（教师线/学生线）走查；像素视觉规范对照；补测首响体感（起算点 A 抽检 3 段） |
| 自动检查 | `npm run lint && npm run typecheck && npx vitest run`（全量） |
| 测试 | 全量回归 + 演示走查 checklist |
| 审核要点 | ①演示脚本铁律：只走确定性字段（理想/承诺/行动），不赌 RAG ②被问"引用准确率"按口径回答（100% 关键字段 + ≥95% 回顾）③首响 P50<2s 有数据支撑 |

---

## 3. 门禁流程与协作规则

1. **模块状态机**：⬜ 未开始 → 🔨 实现中 → ✅ 自动检查通过 → 🧪 测试通过 → 🙋 待人工审核 → 🏁 审核通过。测试失败打回 🔨。
2. **审核请求**：🧪 通过后，向团队提交「模块名 + 测试输出 + 变更文件列表」，由**非本模块负责人**（前端模块交叉审核，或后端/设计师抽审）复核审核要点。
3. **并行规则**：M1↔M4 可并行（仅依赖 M0/M2）；M5 依赖 M3；M6 独立。与后端并行见「后端任务计划书」§3 的接口就绪时间表。
4. **契约变更**：任何第 0 章改动 → 双端同步 + `npm run gen:types` + 后端重新发布 OpenAPI，并通知对方跑对应模块测试。

---

## 4. 前端验收口径（成功指标）

| 指标 | 目标 | 测量 |
|---|---|---|
| 语音首响 P50（起算点 B） | <2s | latency-probe 10 轮中位数 |
| 首响体感（起算点 A） | 抽检无抢话 | 3 段真实停声→出声视频 |
| 图片生成 | <10s | job 轮询计时 |
| 演示完成度 | 两线无阻断 | 教师线 + 学生线走查 |
