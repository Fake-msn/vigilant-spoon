# 小信 · 迁移计划 v3.0（适配 → 融合）

> 版本：v3.0 · 2026-08-06
> 地位：**本文档取代《前端/后端开发任务计划书 v2.0》中所有"未考虑目标项目实际情况"的假设**；契约层细节以 `接口验证调试核对清单.md`（契约 v2.1）为准；逐文件搬迁细节以 `迁移清单-方案B-Vite-SPA改造.md` v1.2 为准；模块指令与门禁沿用《开发任务清单 v3.0》。
> 整改逻辑：**先完成对 web.zip 项目的适配（Phase A），再与新架构融合（Phase F）**——不直接沿用 v2.0 的绿地假设。

---

## 1. 差异分析：v2.0 假设 vs web.zip 现实

v2.0 计划书按"绿地新建 Vite SPA + FastAPI"编写，**把前端当作不存在**。实际上 web.zip 是一个完成度很高的 Next.js 15 高保真 Demo（13 页面、完整设计系统、生成式美术组件、全量演示数据）。不适配条目逐项识别如下：

| # | v2.0 的假设 | web.zip 的现实 | 不适配结论 |
|---|-------------|----------------|------------|
| N1 | 前端从零搭建（FE-M0 新建 Vite 工程） | 已有完整 UI：13 页面 + 设计令牌 + 动效 + 美术组件 | **前端不是"新建"而是"搬迁改造"**；M0 需改为"脚手架+资产平移"（详见迁移清单 §4） |
| N2 | 数据由后端持有 | 全部业务数据硬编码于 `lib/data.ts`（9 组常量），无后端 | 需要**数据迁移路径**（常量 → mocks + 后端 seed），见 §3.4 |
| N3 | 身份走 R1/R2 + session_token | `?s=` URL 透传，无 token 概念，且 mailbox/profile 不透传 | 需新增会话层（stores/session），路由守卫全补 |
| N4 | 语音走 WS 二进制实时协议 | chat 页是 `chatScript` + `setTimeout` 假对话 | 交互内核整体替换，UI 保留（迁移清单 §4.2 已定） |
| N5 | PetState = vitality + mood(cheerful/calm/low) | 用户已拍板三态 daily/gray/cheer | **契约级冲突**，必须按契约 v2.1（核对清单 §0-A1）修订，否则 gen:types 即报错 |
| N6 | 教师端 = 备课 + 课堂面板 + 学生列表 | 用户拍板教师端四功能（备课/学情档案/成长档案/我的课程）为一等验收项 | v2.0 的 M5 范围不足，需扩展 + 新增 R14-R17 接口 |
| N7 | 成长档案含历次得分展示 | 用户拍板：成长档案只做宠物形象墙，**禁评分** | R17 契约禁含评分字段（核对清单 §1-R17 已断言） |
| N8 | 类型由 gen:types 生成 | 类型手写在 `lib/data.ts` | 迁移后类型唯一来源 = OpenAPI；旧类型降级为 mocks 参考 |
| N9 | 工程有 lint/typecheck 门禁 | `next.config.ts` 里 `eslint.ignoreDuringBuilds: true`，零测试 | 门禁从零建立，ignore 写法不得带入新工程 |
| N10 | 渲染形态不限 | 现有项目含 SSR/SSG（mailbox/[id] 静态生成）、`next/font`、`next/image` | 全量改客户端渲染；字体改自托管；图片改原生 img（迁移清单 §5.1 对照表） |

**结论**：v2.0 的**后端部分（B0-B7）基本成立可直接沿用**；**前端部分（M0-M7）必须以"适配 web.zip"为前提重写任务定义**；契约部分必须升级到 v2.1 冻结后，双端才允许开工。

---

## 2. 总路线

```
┌─────────────────────────┐     ┌──────────────────────────────┐
│  Phase A 适配阶段（D1-D5） │ ──▶ │  Phase F 融合阶段（D6-D15）    │
│  web.zip 资产 → Vite SPA  │     │  Vite SPA ↔ FastAPI/WS 真链路 │
│  跑在 mock 上，UI 全保留   │     │  按就绪线分批接入，联调验收    │
└─────────────────────────┘     └──────────────────────────────┘
         前置门禁：契约 v2.1 冻结（D1 上午评审会）
```

**三条总原则**：
1. **以 web.zip 为资产基线**：页面 JSX、设计令牌、动效、美术组件全部保留平移，不推倒重做；v2.0 中与之矛盾的假设以 web.zip 现实为准。
2. **契约先行**：A 阶段开工前冻结契约 v2.1（PetState 三态、R14-R17、错误体、token、WS 事件），冻结后变更只走"改 pydantic → 同步文档 → gen:types → 双端测试"一条流水线。
3. **mock 解耦**：A 阶段全程 `VITE_USE_MOCK=true`，前端不等待后端；F 阶段按就绪线逐批切真接口，每批有回退到 mock 的开关。

---

## 3. Phase A · 适配阶段（D1-D5）

> 目标：web.zip 的全部 UI/交互资产在 Vite SPA 中原样跑通，数据源为 mocks，门禁全绿。
> 详细逐文件映射见 `迁移清单-方案B-Vite-SPA改造.md` §4（30 项），此处为任务化收敛。

### 3.1 任务清单

| ID | 任务 | 产出/依据 | 门禁 |
|----|------|-----------|------|
| A0 | **契约 v2.1 冻结评审**（D1 上午）：PetState 三态、R14-R17、错误 envelope、token、WS 事件、Letter.is_read、分页幂等约定 | 核对清单 §0 修订表 10 项逐条确认；双端计划书第 0 章同步为 v2.1 | 评审签字；之后改动只走变更流水线 |
| A1 | 脚手架：Vite+React18+TS5、vitest、eslint flat（无 ignore）、@tailwindcss/vite、`@/` 别名 | `web-spa/` 工程；迁移清单 §4.1 七项 | `lint && typecheck && vitest` 全绿 |
| A2 | 样式与字体平移：`globals.css` → `src/index.css` 原样；马善政体自托管 `@font-face`（变量名不变） | index.css + 字体文件；视觉对照 splash 页 | 走查无样式回归 |
| A3 | 组件层平移：Icon、art/*（PixelArt/KidAvatar/TeacherAvatar/RegionIcon） | 迁移清单 §4.4；**pixelData 三态配色改造**（daily 保留 / sad→gray 去饱和 / 新增 cheer） | 组件快照测试 |
| A4 | 学生侧页面搬迁：splash / login / identity / home / voice(chat) / letters(mailbox×2) / growth(profile) | 迁移清单 §4.2；identity 改班级码+点选双步；chat 页 UI 保留、内核暂接 mock 事件流 | m1-m3 对应 vitest 用例（mock 下） |
| A5 | 教师侧页面搬迁：entry / setup / lesson(备课) / academic(学情) / growth(宠物墙) / courses / classroom(新页) | 迁移清单 §4.3；academic 改"校内角色+评语"列；growth 删 ScoreBars 等评分元素 | 成长档案页渲染断言无 score（验收指标） |
| A6 | 数据迁移路径落地（见 §3.4） | `src/mocks/data.ts` + `scripts/seed.py` 数据源一致 | seed 与 mocks 字段 diff 为空 |
| A7 | 会话层与路由守卫：stores/session（sessionStorage）、无 token 重定向 `/`、17 条路由表 | 迁移清单 §5.2 | M1 用例③通过 |
| A8 | 适配阶段总验收 | 全量 `lint+typecheck+vitest`；两线 UI 走查（mock 模式） | 全绿 → 进入 Phase F |

### 3.2 前端改造范围（边界声明）

- **改**：路由与框架 API（next/* → react-router）、身份模型（?s= → token）、数据源（常量 → api client/mocks）、PetState 相关展示（三态）、教师端 academic/growth 两页结构、chat 页交互内核。
- **不改**：视觉体系（@theme 令牌、组件类、动画）、美术组件渲染逻辑、页面布局、文案语气、信纸/像素画等设计表达。
- **不做**：journal 模式页面（契约保留枚举，演示期不出页）、真实照片上传（隐私红线，avatar_seed 生成头像）、服务端渲染。

### 3.3 后端接口调整点（适配期需同步启动的契约工作）

| 调整 | 内容 | 落点 |
|------|------|------|
| 修订 | PetState → 三态模型（state/last_growth_at/cheer_until/needs_care/portrait_url，弃 mood/vitality） | 核对清单 §0-A1、§6 |
| 新增 | R14/R15（学情）、R16（课程列表）、R17（班级宠物墙，禁评分） | 核对清单 §1 |
| 修订 | Letter + is_read；ClassInfo + region_key/school/grade/class_no；StudentProfile + role/student_no | 核对清单 §6 |
| 新增 | 错误 envelope + 错误码表；token 机制（st_ opaque/12h/无刷新）；WS 补 vad_start/vad_end/session_end + 关闭码 | 核对清单 §2/§4/§5 |

### 3.4 数据迁移路径（`lib/data.ts` → 新体系）

```
lib/data.ts 常量 ──┬──▶ src/mocks/data.ts     （A 阶段前端数据源 + vitest fixtures）
                   ├──▶ scripts/seed.py      （F 阶段后端演示库；字段按契约 v2.1 改名：
                   │                          dream→ideal、no→student_no、relation→role 枚举映射）
                   └──▶ chatScript 台词       （仅作 FE-M7 演示脚本底稿，不进数据库）
```

- **字段映射表**在 A6 完成时登记进 `迁移清单` §4.5，作为 seed 编写依据；
- **血缘原则**：seed 与 mocks 同源同值，A6 门禁做双端字段 diff；
- 旧 `growthRows.scores`、`academicRows` 等含评分数据只进后端（结算/学情），**教师端成长档案视图契约层屏蔽**。

### 3.5 兼容性处理

| 项 | 处理 |
|----|------|
| 新旧系统并行 | 旧 Next.js demo 不改动，保留为 `/legacy` 视觉兜底与走查参照系（双轨期至演示结束） |
| 契约兼容 | 只增不改不删；OpenAPI diff 进 CI，breaking 即构建失败（迁移清单 §9.3） |
| 旧数据 | 无历史数据（纯前端 Demo），无数据迁移风险；唯一要迁的是"演示剧本语义"→ seed |
| 浏览器兼容 | 语音采集需 Web Audio + getUserMedia，演示设备锁定 Chrome/Edge 最新版，写进演示 checklist |

---

## 4. Phase F · 融合阶段（D6-D15）

> 目标：后端模块按就绪线落地，前端逐批从 mock 切到真接口，全链路联调验收。
> 模块指令细节沿用《开发任务清单 v3.0》（BE-B0~B7 / FE-M0~M7），此处定义**集成顺序与依赖**。

### 4.1 集成顺序与模块依赖

```
F0 后端骨架+契约(B0)        D6        前置：A0 冻结的契约 v2.1
F1 身份融合(B2↔M1)          D7        依赖 F0；前端第一批切真（R1/R2）
F2 语音主线(B1/B3↔M2/M3)    D7-D10    依赖 F0；B1 spike 最高风险 D7 最先做
F3 结算/宠物/档案(B4↔M4)    D10-D12   依赖 F2 转写通路；第三批切真（R3-R6/R17）
F4 写信+教师端(B5/B6↔M5/M6) D12-D14   依赖 F3；第四批切真（R7-R10/R14-R16/R11-R13）
F5 联调验收(B7↔M7)          D14-D15   依赖全部；指标复测+彩排+回滚演练
```

**依赖规则**：
- 前端切真严格按"接口就绪线"（B2→M1、B3→M2/M3、B4→M4、B5→M6、B6→M5），未就绪批次保持 mock；
- 每批切真 = 一次联调矩阵执行（17 接口 × 正常/参数错/404/超时/幂等，判据 = 核对清单 §1-§3）；
- 双轨期任何一批切真失败 → 该批退回 mock，不阻塞后续批次的 mock 态开发。

### 4.2 冲突解决策略

| 冲突场景 | 解决策略 |
|----------|----------|
| 契约争议（文档 vs 代码） | 以后端 `schemas/` + OpenAPI diff 为仲裁；修正结果回登记核对清单（其 §8.4 已定此规则） |
| PetState 新旧模型残留 | A0 冻结后全仓搜索 `vitality/mood` 应为零命中（除变更日志）；B4 宠物状态机按三态转移表实现 |
| 演示数据不一致 | seed 与 mocks 同源（§3.4 血缘）；B7 `--fresh` 清库重 seed |
| 语音链路不达标 | 按迁移清单 §9.6-R2 分级降级：二进制→官方 JSON→文本对话模式；P50>2s 预案（砍注入 token→调 VAD 阈值） |
| 工期不足 | 砍单顺序：RAG → 课堂面板 → 写信模板（教师端四功能与语音主线不可砍） |
| 双端进度错位 | 以"就绪线"为唯一对接节拍；超前的一端继续做 mock 态打磨，禁止私自约定临时契约 |

### 4.3 任务清单（融合期）

| ID | 任务 | 完成标志 |
|----|------|----------|
| F0 | FastAPI 骨架 + schemas v2.1 + SQLite 迁移 + seed + OpenAPI 暴露（B0 全指令） | openapi.json 含 R1-R17；迁移幂等；seed 可查 |
| F1 | R1/R2 上线，identity 页切真；错误态（404/候选姓名）联调 | M1 联调用例全过 |
| F2 | WS 适配层 + RealtimeAgent 三角色 + 语音页切真；首响探针出数 | 单轮语音往返；P50<2s（10 轮）；spike 结论入档 |
| F3 | 结算链 + 宠物三态机 + R3/R4/R5/R6/R17 切真；档案/宠物页联调 | B4 七用例全绿；成长档案无评分断言通过 |
| F4 | 写信 job + RAG 外围 + 课堂状态机 + R7-R10/R14-R16/R11-R13 切真 | B5/B6 用例全绿；教师端四页联调过 |
| F5 | 指标复测 + step 日志 + 演示库清理 + 双线彩排 + **R0/R3 回滚演练** | `docs/acceptance.md` 落数；离线兜底包构建 |

---

## 5. 时间节点总表

> 假设：演示前共 15 个工作日（3 周），双端各 1 人并行。若实际周期不同，按 Phase 比例缩放，**A0 契约冻结不可压缩**。

| 日 | 前端线 | 后端线 | 里程碑 |
|----|--------|--------|--------|
| D1 | A0 契约评审（双端参加）→ A1 脚手架 | 同评审；准备 pyproject | **契约 v2.1 冻结** |
| D2 | A2 样式字体 + A3 组件平移 | B0 骨架/schemas 开工 | |
| D3 | A4 学生侧页面搬迁 | B0 迁移+seed | |
| D4 | A5 教师侧页面搬迁 | B0 收尾+测试 | B0 门禁 |
| D5 | A6 数据迁移 + A7 会话/守卫 + A8 适配验收 | B1 spike 启动 | **Phase A 验收** |
| D6 | mock 态打磨；ws/voice.ts 编写 | B0 完成确认 + B1 继续 | |
| D7 | F1 身份切真 | B2 身份路由 | F1 联调过 |
| D8-D9 | M3 语音页联调 | B1 收尾 → B3 RealtimeAgent 服务 | 单轮语音通 |
| D10 | 语音打磨（互斥/乱序/重连） | B3 测试 + B4 结算链启动 | F2 门禁 |
| D11-D12 | M4 档案/宠物切真 | B4 评分/状态机/幂等 | F3 联调过 |
| D12-D13 | M5 教师端切真 | B5 写信+RAG；B6 课堂 | F4 联调过 |
| D14 | M6 信件+探针；全链路联调启动 | B7 指标复测 | |
| D15 | **双线彩排 + 回滚演练 + 离线包** | acceptance.md 落数 | **总验收** |

## 6. 验收标准

**Phase A 出口**（适配验收）：
1. `lint && typecheck && vitest` 全绿（eslint 无任何 ignore 配置）；
2. 13 页面 + 新增 classroom 页在 mock 模式下双线走查无阻断；
3. 成长档案页渲染树断言不含 score/growth 数字；宠物三态配色切换正确；
4. seed 与 mocks 字段 diff 为空；无 `?s=` 残留（全仓搜索）。

**Phase F 出口**（总验收）：
1. 核对清单（契约 v2.1）§1-§7 逐条勾过；
2. 原任务清单 §8 指标表全过（首响 P50<2s、生图<10s、关键字段引用 100%、信件 100%、宠物 0 跳变、结算幂等）；
3. 联调矩阵 17 接口 × 5 列全过；
4. 回滚演练记录（R0 前端回退 <5min、R3 离线包现场可跑）写入 acceptance.md。

## 7. 风险提示

| 风险 | 等级 | 预案 |
|------|------|------|
| 契约 v2.1 评审拖延，后续全线等待 | 高 | D1 上午强制评审会；争议项按"演示铁律"现场裁决，不留开放式问题 |
| WS 二进制适配失败（最高技术风险） | 高 | B1 spike D5-D7 最先做；失败即按降级链切换并记录，不阻塞 B3 |
| 字体自托管授权问题 | 中 | 备选回退链（行楷/楷体），视觉损失可接受；D2 前必须确认 |
| 双端进度错位导致联调空转 | 中 | 就绪线节拍 + mock 开关；每日 10 分钟站会对表 |
| 演示现场网络不可控 | 中 | R3 离线兜底包每次彩排前重新构建；演示设备提前实测 |
| 儿童隐私合规（语音/成绩数据） | 中 | 演示用虚构学生数据；不接真实照片（avatar_seed）；评审备答口径入演示脚本 |

## 8. 文档关系与废止说明

| 文档 | 状态 |
|------|------|
| 前端/后端开发任务计划书 v2.0 | **部分废止**：前端 M0-M7 的任务定义由本文档 Phase A/F 取代；后端 B0-B7 模块指令继续有效；第 0 章契约须升级 v2.1 |
| 开发任务清单 v3.0 | 继续有效（模块指令与门禁），接口编号扩展至 R17 |
| 迁移清单-方案B v1.2 | 继续有效（逐文件搬迁与回滚预案细节） |
| 接口验证调试核对清单（契约 v2.1） | 继续有效（联调与验收判据） |
| 查漏补缺报告 | 历史文档，其 28 项已被 v2.1 核对清单与本文档吸收 |
