# 个性化班宠系统 + 积分制度设计文档

> 日期：2026-08-08
> 状态：已确认
> 关联模块：成长档案（R3/R4）、课堂教室（R11-R13）、教师端成长档案

## 1. 目标与设计原则

完善个性化班宠系统与积分制度，让激励更有效。核心设计原则：

1. **即时性**：课堂上教师当场加减分，学生立刻看到宠物变化。
2. **可视化**：积分变动直接显示在宠物状态上（饥饿度、心情值、等级）。
3. **竞争性**：小组排行榜（**仅教师端可见**）激发团队荣誉感。
4. **内在驱动力**：宠物等级采用"阶梯式"进化设计，积分越高升级越难，激励持续坚持。

## 2. 积分体系（可配置规则）

### 2.1 预置默认规则
| 行为 | 积分 |
| --- | --- |
| 主动举手发言 | +5 |
| 回答正确 | +10 |
| 作业按时提交 | +8 |
| 作业优秀 | +15 |

> 教师可从成长档案右上角【设置与帮助】进入，对规则进行增删改。

### 2.2 规则管理
- 规则为**班级级**配置（`point_rules` 表），不影响其他班级。
- 教师可新增/编辑/删除规则，自定义规则名与分值（支持正负分）。
- 规则可临时停用（`enabled`）。

## 3. 数据模型

### 3.1 迁移 `0011_points_system.sql`

**积分规则表 `point_rules`**
```sql
CREATE TABLE IF NOT EXISTS point_rules (
    rule_id TEXT PRIMARY KEY,
    class_code TEXT NOT NULL REFERENCES classes(class_code),
    name TEXT NOT NULL,
    points INTEGER NOT NULL,          -- 支持正负分
    category TEXT,                    -- 行为分类（可空）
    enabled INTEGER NOT NULL DEFAULT 1,
    sort INTEGER NOT NULL DEFAULT 0,
    created_at TIMESTAMP NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_point_rules_class ON point_rules(class_code);
```

**积分流水表 `point_ledger`**（可追溯、不可篡改）
```sql
CREATE TABLE IF NOT EXISTS point_ledger (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    student_id TEXT NOT NULL REFERENCES students(student_id),
    class_code TEXT NOT NULL REFERENCES classes(class_code),
    rule_id TEXT,                     -- 关联规则（自定义加分可空）
    name TEXT NOT NULL,               -- 展示名快照
    points INTEGER NOT NULL,          -- 本次变动（±）
    note TEXT,
    created_at TIMESTAMP NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_point_ledger_class ON point_ledger(class_code);
CREATE INDEX IF NOT EXISTS idx_point_ledger_student ON point_ledger(student_id);
```

**小组配置表 `groups`**
```sql
CREATE TABLE IF NOT EXISTS groups (
    group_id TEXT PRIMARY KEY,
    class_code TEXT NOT NULL REFERENCES classes(class_code),
    group_name TEXT NOT NULL,
    color TEXT
);
CREATE INDEX IF NOT EXISTS idx_groups_class ON groups(class_code);
```

**`students` 表新增列**
```sql
ALTER TABLE students ADD COLUMN group_id TEXT;
```

**`growth_records` 表扩展积分与宠物状态字段**
```sql
ALTER TABLE growth_records ADD COLUMN points_total INTEGER NOT NULL DEFAULT 0;
ALTER TABLE growth_records ADD COLUMN level INTEGER NOT NULL DEFAULT 1;
ALTER TABLE growth_records ADD COLUMN hunger INTEGER NOT NULL DEFAULT 50;
ALTER TABLE growth_records ADD COLUMN mood INTEGER NOT NULL DEFAULT 60;
ALTER TABLE growth_records ADD COLUMN last_points_at TIMESTAMP;
```

### 3.2 字段语义
- `points_total`：学生累计积分（只增不减的累计值，用于等级判定）。
- `level`：宠物等级，由 `points_total` 阶梯推导。
- `hunger`：饥饿度 0-100，加分时下降（喂食），长期无积分上升。
- `mood`：心情 0-100，加分上升，长期无积分下降。
- `last_points_at`：最近一次获得积分时间，用于饥饿/心情衰减判定。

## 4. 宠物等级"阶梯式进化"

### 4.1 等级阈值（阶梯式递增大纲）
| 等级 | 累计积分 | 说明 |
| --- | --- | --- |
| Lv.1 | 0 | 初始 |
| Lv.2 | 30 | |
| Lv.3 | 70 | |
| Lv.4 | 120 | |
| Lv.5 | 180 | |
| Lv.6 | 250 | 每级阈值递增，升级越来越难 |

> 阈值表集中定义在 `services/pet.py` 中，便于调整。等级每次提升触发进化庆祝。

### 4.2 等级提升联动
- 加分后重算 `level`，若提升则记录一次"升级事件"（写入流水 `note` 或独立事件）。
- 前端在学生端/教师端展示升级庆祝动效。

## 5. 饥饿度 / 心情值可视化

### 5.1 变化规则
- **加分**：`hunger` 下降（喂食）、`mood` 上升。
- **长期无积分**：`hunger` 上升、`mood` 下降。
- 阈值联动：`hunger` 高于阈值 → 进入"饥饿/需关注"可视化状态（补充现有 gray 态）。

### 5.2 展示
- 学生端成长页宠物卡片：直接显示积分、等级、饥饿度、心情值四要素。
- 教师端成长档案列表：显示每只宠物等级与饥饿度状态。

## 6. 后端 API

所有接口沿用现有鉴权（教师需 `user.class_code == class_code`）。

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/classes/{code}/points/award` | 教师即时加减分（写流水 + 更新生长记录） |
| GET | `/classes/{code}/points/rules` | 获取班级积分规则 |
| PUT | `/classes/{code}/points/rules` | 批量更新规则（增/删/改） |
| GET | `/classes/{code}/points/overview` | 课堂加分面板数据（学生+规则+小组） |
| GET | `/classes/{code}/groups` | 获取小组及成员 |
| PUT | `/classes/{code}/groups` | 分组配置（建组/分配成员） |
| GET | `/classes/{code}/leaderboard` | 小组排行榜（仅教师端调用） |
| GET | `/students/{id}/points` | 个人积分流水 |

### 6.1 加分请求体示例
```json
{
  "student_id": "LTZ2024-01",
  "rule_id": "hand-raise",
  "points": 5,
  "name": "主动举手发言",
  "note": "语文课"
}
```

### 6.2 加分处理器职责
1. 校验学生归属与鉴权。
2. 写入 `point_ledger` 流水。
3. 更新 `growth_records`：`points_total += points`、`hunger`/`mood` 联动、`last_points_at`。
4. 重算并回写 `level`，若升级则产生升级事件。
5. 返回最新宠物状态（供前端即时刷新）。

## 7. 前端

### 7.1 课堂教室页（`TeacherClassroomPage.tsx`）
- 新增"积分/奖励"面板：选择学生 → 选择规则（或自定义分值）→ 加分。
- 加分后即时刷新，学生端可看到宠物变化。

### 7.2 教师端成长档案页（`TeacherGrowthPage.tsx`）
- 标题栏右上角新增【设置与帮助】按钮。
- 弹窗三块：
  1. **积分规则**：增删改可用规则。
  2. **小组配置**：创建小组、分配学生。
  3. **小组排行榜**：按小组累计积分排序（仅教师端可见）。

### 7.3 学生端成长页（`GrowthPage.tsx`）
- 宠物卡片新增：积分、等级、饥饿度、心情值展示。
- 等级进化庆祝动效。
- 积分流水查看。

## 8. 测试

### 8.1 后端（pytest）
- 加分写流水、等级阶梯升级、饥饿/心情联动。
- 规则增删改。
- 小组配置与小组排行榜。
- 鉴权与越权校验。

### 8.2 前端（vitest）
- 课堂加分面板组件。
- 教师【设置与帮助】弹窗（规则/小组/排行榜）。
- 学生端等级、饥饿度、心情值渲染。

## 9. 范围说明
- 小组排行榜**仅教师端可见**，学生端不展示。
- 个人宠物机制保留，小组为积分汇总维度，不做共享宠物。
- 本设计不包含共享宠物模型、通知推送等后续增强。